import { LoadingIndicator, LocalesMenuButton } from "react-admin";

const AppBarToolbar = () => {
  return (
    <>
      <LoadingIndicator />
      <LocalesMenuButton />
    </>
  );
};

export default AppBarToolbar;
